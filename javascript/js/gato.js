var operador ="";


function numeros(num)
{




if 	








	//Escribir en el operando1
	var valor=document.gato
	if (operador=="") //vaciamos la caja

	{
		if (valor=="X")	//concatenar los valores de num con los del operando1
			document.calculadora.operando1.value =
			document.calculadora.operando1.value + num;
		
		else {
			if (valor=="O")	//concatenar los valores de num con los del operando1
			document.calculadora.operando2.value =
			document.calculadora.operando2value + num;
		}
			
	}


}